# Priority map

Special version of map which is useful as a building block for queues.

> A priority map is very similar to a sorted map, but whereas a sorted map produces a sequence of the entries sorted by key, a priority map produces the entries sorted by value.

GitHub: [clojure/clr.data.priority-map](https://github.com/clojure/clr.data.priority-map)

Nuget: https://www.nuget.org/packages/clojure.data.priority-map

Csproj: `<PackageReference Include="clojure.data.priority-map" Version="1.2.0" />`

deps: `io.github.clojure/clr.data.priority-map {:git/tag "v1.2.0" :git/sha "1c22751"}`
